pandas
numpy
pyarrow
scikit-learn
matplotlib

# Sentiment model (Objective 1)
transformers
datasets
accelerate

# torch is installed separately with a CUDA build matching the local GPU:
# pip install --index-url https://download.pytorch.org/whl/cu126 torch==2.13.0+cu126
torch
